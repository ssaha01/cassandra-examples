// Creating key space 'test'CREATE KEYSPACE IF NOT EXISTS testWITH REPLICATION = { 'class' : 'SimpleStrategy', 'replication_factor' : 1}AND DURABLE_WRITES = true;// Creating column falily 'flights' in key space 'test'CREATE TABLE flights (            	ID int PRIMARY KEY,            	YEAR int,          	            	DAY_OF_MONTH int,            	FL_DATE timestamp,            	AIRLINE_ID int,            	CARRIER varchar,            	FL_NUM int,            	ORIGIN_AIRPORT_ID int,            	ORIGIN varchar,            	ORIGIN_CITY_NAME varchar,            	ORIGIN_STATE_ABR varchar,            	DEST varchar,            	DEST_CITY_NAME varchar,            	DEST_STATE_ABR varchar,            	DEP_TIME timestamp,            	ARR_TIME timestamp,            	ACTUAL_ELAPSED_TIME timestamp,            	AIR_TIME timestamp,            	DISTANCE int);// Creating column falily 'flights_by_origin_airport' in key space 'test'CREATE TABLE flights_by_origin_airport (            	FL_DATE timestamp,            	AIRLINE_ID int,            	CARRIER varchar,            	FL_NUM int,            	ORIGIN_AIRPORT_ID int,            	DEP_TIME timestamp,            	PRIMARY KEY (ORIGIN_AIRPORT_ID, DEP_TIME));// Creating column falily 'flights_by_flight_num' in key space 'test'CREATE TABLE flights_by_flight_num (            	CARRIER varchar,            	FL_NUM int,            	ORIGIN varchar,            	DEST varchar,            	AIR_TIME timestamp,            	PRIMARY KEY (FL_NUM, AIR_TIME));mvn clean compile assembly:single